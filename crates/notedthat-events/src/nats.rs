//! The `nats` adapter: one `JetStream` stream shared by every replica.
//!
//! Filled in with the adapter; this stub keeps the feature gate compiling.

/// Placeholder until the adapter lands.
pub struct NatsPublisher;
