# Cargo build artifacts
/target/

# Rust backup files
**/*.rs.bk

# macOS
.DS_Store

# IDE
.idea/
.vscode/

# Agent scratch directory (per-developer plans, drafts, evidence, notepads).
# Never committed — belongs to the local workflow, not the repo.
.sisyphus/
.omo/

.env

# Core dumps (a 5 GiB stress run that aborts leaves one here)
core
core.*

# Sentry Warden local runs (see DEVELOPMENT.md): findings JSON and bake-off output
warden-findings.json
bakeoff/

# Warden run logs written next to the checkout by local CLI runs
.warden/
